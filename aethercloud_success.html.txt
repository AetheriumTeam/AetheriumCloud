<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Успешная покупка - AetherCloud</title>
    
    <style>
        /* ============================================
           ОБЩИЕ СТИЛИ
           ============================================ */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #0a1628 0%, #1a2f4f 100%);
            color: #fff;
            min-height: 100vh; /* Высота на весь экран */
            display: flex; /* Flexbox */
            align-items: center; /* По центру вертикально */
            justify-content: center; /* По центру горизонтально */
            padding: 20px; /* Отступы по краям */
        }
        /* ============================================ */


        /* ============================================
           КОНТЕЙНЕР УСПЕХА (success-container)
           Главная обертка с информацией о покупке
           ============================================ */
        .success-container {
            background: rgba(26, 47, 79, 0.8); /* Полупрозрачный синий фон */
            padding: 50px 40px; /* Внутренние отступы */
            border-radius: 20px; /* Скругленные углы */
            border: 2px solid rgba(255, 200, 87, 0.3); /* Золотая рамка */
            backdrop-filter: blur(10px); /* Размытие фона */
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.5); /* Тень */
            max-width: 600px; /* Максимальная ширина */
            width: 100%; /* Ширина 100% */
            text-align: center; /* Текст по центру */
        }
        /* ============================================ */


        /* ============================================
           ИКОНКА УСПЕХА (success-icon)
           Большая галочка в круге
           ============================================ */
        .success-icon {
            width: 100px; /* Ширина */
            height: 100px; /* Высота */
            margin: 0 auto 30px; /* Центрируем и отступ снизу */
            background: linear-gradient(135deg, #4ade80 0%, #22c55e 100%); /* Зеленый градиент */
            border-radius: 50%; /* Круглая форма */
            display: flex; /* Flexbox */
            align-items: center; /* По центру вертикально */
            justify-content: center; /* По центру горизонтально */
            font-size: 50px; /* Размер иконки */
            box-shadow: 0 10px 40px rgba(74, 222, 128, 0.4); /* Зеленая тень */
            animation: scaleIn 0.5s ease-out; /* Анимация появления */
        }

        /* Анимация масштабирования */
        @keyframes scaleIn {
            from {
                transform: scale(0); /* Начинаем с 0 размера */
                opacity: 0; /* Прозрачный */
            }
            to {
                transform: scale(1); /* Нормальный размер */
                opacity: 1; /* Непрозрачный */
            }
        }
        /* ============================================ */


        /* ============================================
           ЗАГОЛОВОК И ПОДЗАГОЛОВОК
           ============================================ */
        .success-container h1 {
            font-size: 36px; /* Размер заголовка */
            margin-bottom: 15px; /* Отступ снизу */
            background: linear-gradient(135deg, #ffc857 0%, #ffeb3b 100%); /* Золотой градиент */
            -webkit-background-clip: text; /* Применяем к тексту */
            -webkit-text-fill-color: transparent; /* Прозрачный текст */
            animation: fadeInUp 0.6s 0.2s backwards; /* Анимация появления */
        }

        .success-container p {
            font-size: 18px; /* Размер текста */
            color: #b0c4de; /* Светло-голубой */
            margin-bottom: 30px; /* Отступ снизу */
            line-height: 1.6; /* Высота строки */
            animation: fadeInUp 0.6s 0.3s backwards; /* Анимация */
        }

        /* Анимация появления снизу вверх */
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        /* ============================================ */


        /* ============================================
           ИНФОРМАЦИЯ О ЗАКАЗЕ (order-info)
           Детали купленного тарифа
           ============================================ */
        .order-info {
            background: rgba(10, 22, 40, 0.6); /* Темный фон */
            padding: 25px; /* Внутренние отступы */
            border-radius: 15px; /* Скругленные углы */
            border: 2px solid rgba(255, 200, 87, 0.2); /* Золотая рамка */
            margin-bottom: 30px; /* Отступ снизу */
            text-align: left; /* Текст по левому краю */
            animation: fadeInUp 0.6s 0.4s backwards; /* Анимация */
        }

        .order-info h3 {
            color: #ffc857; /* Золотой цвет */
            font-size: 20px; /* Размер заголовка */
            margin-bottom: 20px; /* Отступ снизу */
            text-align: center; /* По центру */
        }

        /* Строка информации */
        .info-row {
            display: flex; /* Flexbox */
            justify-content: space-between; /* Распределяем по краям */
            padding: 12px 0; /* Отступы сверху/снизу */
            border-bottom: 1px solid rgba(255, 200, 87, 0.1); /* Нижняя линия */
        }

        .info-row:last-child {
            border-bottom: none; /* Убираем линию у последней строки */
        }

        .info-label {
            color: #b0c4de; /* Светло-голубой */
            font-size: 14px; /* Размер текста */
        }

        .info-value {
            color: #fff; /* Белый цвет */
            font-weight: 600; /* Полужирный */
            font-size: 14px; /* Размер текста */
        }
        /* ============================================ */


        /* ============================================
           ИНФОРМАЦИОННЫЙ БЛОК (info-box)
           Важная информация о сервере
           ============================================ */
        .info-box {
            background: rgba(79, 70, 229, 0.2); /* Фиолетовый полупрозрачный фон */
            border: 2px solid rgba(79, 70, 229, 0.4); /* Фиолетовая рамка */
            padding: 20px; /* Внутренние отступы */
            border-radius: 15px; /* Скругленные углы */
            margin-bottom: 30px; /* Отступ снизу */
            animation: fadeInUp 0.6s 0.5s backwards; /* Анимация */
        }

        .info-box p {
            margin: 0; /* Убираем отступы */
            font-size: 14px; /* Размер текста */
            color: #e0e7ff; /* Светло-фиолетовый текст */
        }

        .info-box strong {
            color: #ffc857; /* Золотой цвет для выделения */
        }
        /* ============================================ */


        /* ============================================
           КНОПКИ ДЕЙСТВИЙ (action-buttons)
           Кнопки для перехода к управлению
           ============================================ */
        .action-buttons {
            display: flex; /* Размещаем в ряд */
            gap: 15px; /* Отступ между кнопками */
            margin-top: 30px; /* Отступ сверху */
            animation: fadeInUp 0.6s 0.6s backwards; /* Анимация */
        }

        .btn {
            flex: 1; /* Занимают равное место */
            padding: 15px 30px; /* Внутренние отступы */
            border-radius: 50px; /* Очень скругленные края */
            text-decoration: none; /* Без подчеркивания */
            font-weight: 700; /* Жирный шрифт */
            font-size: 16px; /* Размер текста */
            transition: all 0.3s; /* Плавный переход */
            cursor: pointer; /* Курсор-указатель */
            border: none; /* Без рамки */
        }

        /* Основная кнопка (золотая) */
        .btn-primary {
            background: linear-gradient(135deg, #ffc857 0%, #ffeb3b 100%); /* Золотой градиент */
            color: #0a1628; /* Темный текст */
            box-shadow: 0 10px 30px rgba(255, 200, 87, 0.3); /* Тень */
        }

        .btn-primary:hover {
            transform: translateY(-3px); /* Поднимается */
            box-shadow: 0 15px 40px rgba(255, 200, 87, 0.5); /* Увеличенная тень */
        }

        /* Вторичная кнопка (с рамкой) */
        .btn-secondary {
            background: transparent; /* Прозрачный фон */
            border: 2px solid #ffc857; /* Золотая рамка */
            color: #ffc857; /* Золотой текст */
        }

        .btn-secondary:hover {
            background: rgba(255, 200, 87, 0.1); /* Слегка золотой фон */
            transform: translateY(-3px); /* Поднимается */
        }
        /* ============================================ */


        /* ============================================
           АДАПТАЦИЯ ПОД МОБИЛЬНЫЕ
           ============================================ */
        @media (max-width: 768px) {
            .success-container {
                padding: 30px 25px; /* Меньшие отступы */
            }

            .success-container h1 {
                font-size: 28px; /* Меньший заголовок */
            }

            .action-buttons {
                flex-direction: column; /* Кнопки в столбик */
            }

            .success-icon {
                width: 80px; /* Меньший размер иконки */
                height: 80px;
                font-size: 40px;
            }
        }
        /* ============================================ */
    </style>
</head>
<body>
    <!-- ============================================
         КОНТЕЙНЕР УСПЕШНОЙ ПОКУПКИ
         ============================================ -->
    <div class="success-container">
        <!-- Иконка успеха (галочка) -->
        <div class="success-icon">✓</div>
        
        <!-- Заголовок и описание -->
        <h1>Оплата успешна!</h1>
        <p>Спасибо за покупку! Ваш сервер уже настраивается и будет готов через несколько секунд.</p>

        <!-- Информация о заказе -->
        <div class="order-info">
            <h3>Детали заказа</h3>
            
            <div class="info-row">
                <span class="info-label">Тариф:</span>
                <span class="info-value">Продвинутый</span>
            </div>
            
            <div class="info-row">
                <span class="info-label">RAM:</span>
                <span class="info-value">4 GB</span>
            </div>
            
            <div class="info-row">
                <span class="info-label">CPU:</span>
                <span class="info-value">4 ядра</span>
            </div>
            
            <div class="info-row">
                <span class="info-label">SSD:</span>
                <span class="info-value">25 GB</span>
            </div>
            
            <div class="info-row">
                <span class="info-label">Максимум игроков:</span>
                <span class="info-value">До 30</span>
            </div>
            
            <div class="info-row">
                <span class="info-label">Период:</span>
                <span class="info-value">1 месяц</span>
            </div>
            
            <div class="info-row">
                <span class="info-label">Сумма:</span>
                <span class="info-value">599 ₽</span>
            </div>
        </div>

        <!-- Информационный блок -->
        <div class="info-box">
            <p>
                📧 <strong>Письмо отправлено!</strong> Мы отправили подтверждение и данные для подключения на вашу почту. Проверьте папку "Входящие" или "Спам".
            </p>
        </div>

        <!-- Кнопки действий -->
        <div class="action-buttons">
            <!-- Переход к управлению сервером -->
            <a href="dashboard.html" class="btn btn-primary">Управление сервером</a>
            <!-- Возврат на главную -->
            <a href="index.html" class="btn btn-secondary">На главную</a>
        </div>
    </div>
</body>
</html>