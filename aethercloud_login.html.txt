<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Вход - AetherCloud</title>
    
    <style>
        /* ============================================
           ОБЩИЕ СТИЛИ
           ============================================ */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #0a1628 0%, #1a2f4f 100%);
            color: #fff;
            min-height: 100vh; /* Минимальная высота на весь экран */
            display: flex; /* Flexbox для центрирования */
            align-items: center; /* По центру вертикально */
            justify-content: center; /* По центру горизонтально */
        }
        /* ============================================ */


        /* ============================================
           КОНТЕЙНЕР ФОРМЫ ВХОДА (login-container)
           Обертка для формы с фоном
           ============================================ */
        .login-container {
            background: rgba(26, 47, 79, 0.8); /* Полупрозрачный синий фон */
            padding: 50px 40px; /* Внутренние отступы */
            border-radius: 20px; /* Скругленные углы */
            border: 2px solid rgba(255, 200, 87, 0.3); /* Золотая рамка */
            backdrop-filter: blur(10px); /* Размытие фона */
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.5); /* Тень */
            width: 100%; /* Ширина 100% */
            max-width: 450px; /* Максимальная ширина 450px */
        }
        /* ============================================ */


        /* ============================================
           ЛОГОТИП НА СТРАНИЦЕ ВХОДА (login-logo)
           ============================================ */
        .login-logo {
            text-align: center; /* По центру */
            margin-bottom: 30px; /* Отступ снизу */
        }

        .login-logo img {
            width: 80px; /* Ширина логотипа */
            height: 80px; /* Высота логотипа */
            margin-bottom: 15px; /* Отступ снизу */
        }

        .login-logo h1 {
            font-size: 32px; /* Размер текста */
            background: linear-gradient(135deg, #ffc857 0%, #ffeb3b 100%); /* Градиент */
            -webkit-background-clip: text; /* Применяем к тексту */
            -webkit-text-fill-color: transparent; /* Прозрачный текст */
            margin-bottom: 10px; /* Отступ снизу */
        }

        .login-logo p {
            color: #b0c4de; /* Светло-голубой цвет */
            font-size: 16px; /* Размер текста */
        }
        /* ============================================ */


        /* ============================================
           ФОРМА ВХОДА (login-form)
           Поля ввода и кнопки
           ============================================ */
        .login-form {
            margin-top: 30px; /* Отступ сверху */
        }

        /* Группа полей - объединяет label и input */
        .form-group {
            margin-bottom: 25px; /* Отступ снизу между полями */
        }

        /* Подписи к полям (Email, Пароль) */
        .form-group label {
            display: block; /* Занимает всю ширину */
            margin-bottom: 8px; /* Отступ снизу */
            color: #ffc857; /* Золотой цвет */
            font-weight: 600; /* Полужирный */
            font-size: 14px; /* Размер текста */
        }

        /* Поля ввода (input) */
        .form-group input {
            width: 100%; /* Ширина 100% */
            padding: 15px; /* Внутренние отступы */
            background: rgba(10, 22, 40, 0.6); /* Темный полупрозрачный фон */
            border: 2px solid rgba(255, 200, 87, 0.3); /* Золотая рамка */
            border-radius: 10px; /* Скругленные углы */
            color: #fff; /* Белый текст */
            font-size: 16px; /* Размер текста */
            transition: all 0.3s; /* Плавный переход */
        }

        /* Когда поле в фокусе (пользователь кликнул) */
        .form-group input:focus {
            outline: none; /* Убираем стандартную обводку */
            border-color: #ffc857; /* Яркая золотая рамка */
            background: rgba(10, 22, 40, 0.8); /* Более насыщенный фон */
            box-shadow: 0 0 20px rgba(255, 200, 87, 0.3); /* Свечение */
        }

        /* Placeholder текст (подсказка в поле) */
        .form-group input::placeholder {
            color: #7a8fa8; /* Приглушенный голубой */
        }
        /* ============================================ */


        /* ============================================
           ЧЕКБОКС "ЗАПОМНИТЬ МЕНЯ" (remember-forgot)
           ============================================ */
        .remember-forgot {
            display: flex; /* Размещаем в ряд */
            justify-content: space-between; /* Распределяем по краям */
            align-items: center; /* Выравнивание по центру */
            margin-bottom: 25px; /* Отступ снизу */
            font-size: 14px; /* Размер текста */
        }

        .remember-forgot label {
            color: #b0c4de; /* Светло-голубой */
            cursor: pointer; /* Курсор-указатель */
        }

        .remember-forgot input[type="checkbox"] {
            margin-right: 8px; /* Отступ справа */
            cursor: pointer; /* Курсор-указатель */
        }

        /* Ссылка "Забыли пароль?" */
        .remember-forgot a {
            color: #ffc857; /* Золотой цвет */
            text-decoration: none; /* Без подчеркивания */
            transition: all 0.3s; /* Плавный переход */
        }

        .remember-forgot a:hover {
            color: #ffeb3b; /* Светлее при наведении */
        }
        /* ============================================ */


        /* ============================================
           КНОПКА ВХОДА (login-button)
           ============================================ */
        .login-button {
            width: 100%; /* Ширина 100% */
            padding: 15px; /* Внутренние отступы */
            background: linear-gradient(135deg, #ffc857 0%, #ffeb3b 100%); /* Золотой градиент */
            border: none; /* Без рамки */
            border-radius: 50px; /* Очень скругленные края */
            color: #0a1628; /* Темный текст */
            font-size: 18px; /* Размер текста */
            font-weight: 700; /* Жирный шрифт */
            cursor: pointer; /* Курсор-указатель */
            transition: all 0.3s; /* Плавный переход */
            box-shadow: 0 10px 30px rgba(255, 200, 87, 0.3); /* Тень */
        }

        .login-button:hover {
            transform: translateY(-3px); /* Поднимается при наведении */
            box-shadow: 0 15px 40px rgba(255, 200, 87, 0.5); /* Увеличенная тень */
        }

        .login-button:active {
            transform: translateY(0); /* Опускается при клике */
        }
        /* ============================================ */


        /* ============================================
           РАЗДЕЛИТЕЛЬ "ИЛИ" (divider)
           ============================================ */
        .divider {
            display: flex; /* Размещаем элементы в ряд */
            align-items: center; /* По центру вертикально */
            margin: 30px 0; /* Отступы сверху/снизу */
        }

        .divider::before,
        .divider::after {
            content: ''; /* Пустой элемент */
            flex: 1; /* Занимает все доступное место */
            height: 1px; /* Высота линии */
            background: rgba(255, 200, 87, 0.3); /* Золотая линия */
        }

        .divider span {
            padding: 0 15px; /* Отступы по бокам */
            color: #b0c4de; /* Светло-голубой */
            font-size: 14px; /* Размер текста */
        }
        /* ============================================ */


        /* ============================================
           СОЦИАЛЬНЫЕ КНОПКИ (social-login)
           Вход через Discord, Google и т.д.
           ============================================ */
        .social-login {
            display: flex; /* Размещаем в ряд */
            gap: 15px; /* Отступ между кнопками */
            margin-bottom: 30px; /* Отступ снизу */
        }

        .social-btn {
            flex: 1; /* Занимают равное место */
            padding: 12px; /* Внутренние отступы */
            background: rgba(10, 22, 40, 0.6); /* Темный фон */
            border: 2px solid rgba(255, 200, 87, 0.3); /* Золотая рамка */
            border-radius: 10px; /* Скругленные углы */
            color: #fff; /* Белый текст */
            font-size: 14px; /* Размер текста */
            cursor: pointer; /* Курсор-указатель */
            transition: all 0.3s; /* Плавный переход */
            display: flex; /* Flexbox для выравнивания */
            align-items: center; /* По центру вертикально */
            justify-content: center; /* По центру горизонтально */
            gap: 8px; /* Отступ между иконкой и текстом */
        }

        .social-btn:hover {
            border-color: #ffc857; /* Яркая рамка при наведении */
            background: rgba(255, 200, 87, 0.1); /* Светлый фон */
            transform: translateY(-2px); /* Поднимается */
        }
        /* ============================================ */


        /* ============================================
           РЕГИСТРАЦИЯ (signup-link)
           Ссылка "Нет аккаунта? Зарегистрироваться"
           ============================================ */
        .signup-link {
            text-align: center; /* По центру */
            margin-top: 25px; /* Отступ сверху */
            color: #b0c4de; /* Светло-голубой */
            font-size: 14px; /* Размер текста */
        }

        .signup-link a {
            color: #ffc857; /* Золотой цвет */
            text-decoration: none; /* Без подчеркивания */
            font-weight: 600; /* Полужирный */
            transition: all 0.3s; /* Плавный переход */
        }

        .signup-link a:hover {
            color: #ffeb3b; /* Светлее при наведении */
        }
        /* ============================================ */


        /* ============================================
           КНОПКА НАЗАД (back-button)
           ============================================ */
        .back-button {
            display: inline-block; /* Блочный в строке */
            margin-bottom: 20px; /* Отступ снизу */
            color: #ffc857; /* Золотой цвет */
            text-decoration: none; /* Без подчеркивания */
            font-size: 14px; /* Размер текста */
            transition: all 0.3s; /* Плавный переход */
        }

        .back-button:hover {
            color: #ffeb3b; /* Светлее при наведении */
            transform: translateX(-5px); /* Сдвигается влево */
        }
        /* ============================================ */


        /* ============================================
           АДАПТАЦИЯ ПОД МОБИЛЬНЫЕ
           ============================================ */
        @media (max-width: 768px) {
            .login-container {
                margin: 20px; /* Отступы по краям */
                padding: 30px 25px; /* Меньшие отступы */
            }

            .login-logo h1 {
                font-size: 28px; /* Меньший заголовок */
            }

            .social-login {
                flex-direction: column; /* Кнопки в столбик */
            }
        }
        /* ============================================ */
    </style>
</head>
<body>
    <!-- ============================================
         КОНТЕЙНЕР ФОРМЫ ВХОДА
         ============================================ -->
    <div class="login-container">
        <!-- Кнопка возврата на главную -->
        <a href="index.html" class="back-button">← Назад на главную</a>
        
        <!-- Логотип и заголовок -->
        <div class="login-logo">
            <img src="logo.png" alt="AetherCloud">
            <h1>AetherCloud</h1>
            <p>Войдите в свой аккаунт</p>
        </div>

        <!-- ФОРМА ВХОДА -->
        <form class="login-form" action="dashboard.html" method="get">
            <!-- Поле Email -->
            <div class="form-group">
                <label for="email">Email адрес</label>
                <input 
                    type="email" 
                    id="email" 
                    name="email" 
                    placeholder="example@mail.com" 
                    required
                >
            </div>

            <!-- Поле Пароль -->
            <div class="form-group">
                <label for="password">Пароль</label>
                <input 
                    type="password" 
                    id="password" 
                    name="password" 
                    placeholder="Введите пароль" 
                    required
                >
            </div>

            <!-- Запомнить меня и Забыли пароль -->
            <div class="remember-forgot">
                <label>
                    <input type="checkbox" name="remember">
                    Запомнить меня
                </label>
                <a href="#">Забыли пароль?</a>
            </div>

            <!-- Кнопка входа -->
            <button type="submit" class="login-button">Войти</button>
        </form>

        <!-- Разделитель -->
        <div class="divider">
            <span>или войдите через</span>
        </div>

        <!-- Вход через соцсети -->
        <div class="social-login">
            <button class="social-btn">
                💬 Discord
            </button>
            <button class="social-btn">
                📧 Google
            </button>
        </div>

        <!-- Ссылка на регистрацию -->
        <div class="signup-link">
            Нет аккаунта? <a href="register.html">Зарегистрироваться</a>
        </div>
    </div>
</body>
</html>